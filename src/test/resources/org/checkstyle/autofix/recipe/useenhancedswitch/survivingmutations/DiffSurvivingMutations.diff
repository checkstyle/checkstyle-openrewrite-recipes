--- src/test/resources/org/checkstyle/autofix/recipe/useenhancedswitch/survivingmutations/InputSurvivingMutations.java
+++ src/test/resources/org/checkstyle/autofix/recipe/useenhancedswitch/survivingmutations/OutputSurvivingMutations.java
@@ -13,13 +13,12 @@
 
 package org.checkstyle.autofix.recipe.useenhancedswitch.survivingmutations;
 
-public class InputSurvivingMutations {
+public class OutputSurvivingMutations {
   
     void m1(int x) {
-        // violation below, 'Switch can be replaced with enhanced switch'
         switch (x) {
-            case 1: break;
-            default: break;
+            case 1 -> {}
+            default -> {}
         }
     }
 
@@ -38,10 +37,9 @@
     }
 
     int m2(int x) {
-               // violation below, 'Switch can be replaced with enhanced switch'
                return switch (x) {
-            case 1: yield 1;
-            default: yield 0;
+            case 1 -> 1;
+            default -> 0;
         };
     }
 
@@ -60,24 +58,21 @@
     }
 
     void m3(int x, int y) {
-        // violation below, 'Switch can be replaced with enhanced switch'
         switch (x) {
-            case 1:
-                // violation below, 'Switch can be replaced with enhanced switch'
+            case 1 -> {
                 switch (y) {
-                    case 2: break;
-                    default: break;
+                    case 2 -> {}
+                    default -> {}
                 }
-                break;
-            default: break;
+            }
+            default -> {}
         }
     }
 
     void m4(int x) {
-    // violation below, 'Switch can be replaced with enhanced switch'
     switch (x) {
-    case 1: break;
-    default: break;
+    case 1 -> {}
+    default -> {}
     }
 
                                
@@ -90,10 +85,9 @@
     int m5(int x, int y) {
         return switch (x) { // (suppressed violation)
             case 1: // (suppressed violation)
-                // violation below, 'Switch can be replaced with enhanced switch'
                 yield switch (y) {
-                    case 2: yield 2;
-                    default: yield 0;
+                    case 2 -> 2;
+                    default -> 0;
                 };
             default: yield 0;
         };
