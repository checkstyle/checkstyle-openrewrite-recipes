--- src/test/resources/org/checkstyle/autofix/recipe/useenhancedswitch/mutationtests14/InputMutationTests14.java
+++ src/test/resources/org/checkstyle/autofix/recipe/useenhancedswitch/mutationtests14/OutputMutationTests14.java
@@ -7,102 +7,70 @@
 */
 package org.checkstyle.autofix.recipe.useenhancedswitch.mutationtests14;
 
-public class InputMutationTests14 {
+public class OutputMutationTests14 {
     enum Color { RED, GREEN, BLUE }
 
     void test(int x) {
         int y;
-        switch (x) { // violation 'Switch can be replaced with enhanced switch.'
-            case 1:
-            case 2:
-                y = 1;
-                break;
-            case 3:
+        switch (x) {
+            case 1, 2 -> y = 1;
+            case 3 -> {
                 y = 2;
                 x = 3;
-                break;
-            default:
-                y = 0;
-                break;
+            }
+            default -> y = 0;
         }
     }
 
     void testEnum(Color c) {
-        int x;
-        switch (c) { // violation 'Switch can be replaced with enhanced switch.'
-            case RED:
-                x = 1;
-                break;
-            case GREEN:
-                x = 2;
-                break;
-            case BLUE:
-                x = 3;
-                break;
-        }
+        int x = switch (c) {
+            case RED -> 1;
+            case GREEN -> 2;
+            case BLUE -> 3;
+        };
     }
 
     void testIndent(int x) {
-        int y;
-        switch (x) { // violation 'Switch can be replaced with enhanced switch.'
-            case 1:
-                y = 1;
-                break;
-            default:
-                y = 0;
-                break;
-        }
+        int y = switch (x) {
+            case 1 -> 1;
+            default -> 0;
+        };
     }
 
     int testReturn(int x) {
-        switch (x) { // violation 'Switch can be replaced with enhanced switch.'
-            case 1:
-                return 1;
-            default:
-                return 0;
-        }
+        return switch (x) {
+            case 1 -> 1;
+            default -> 0;
+        };
     }
 
     void testIndent2(int x) {
-        int y;
-        switch (x) { // violation 'Switch can be replaced with enhanced switch.'
-            case 1:
-          y = 1;
-                break;
-            default:
-                y = 0;
-                break;
-        }
+        int y = switch (x) {
+            case 1 -> 1;
+            default -> 0;
+        };
     }
 
     void testIndent3(int x) {
-        int y;
-        switch (x) { // violation 'Switch can be replaced with enhanced switch.'
-            case 1:
-y = 1;
-                break;
-            default:
-                y = 0;
-                break;
-        }
+        int y = switch (x) {
+            case 1 -> 1;
+            default -> 0;
+        };
     }
 
     void testNoNewline(int x) {
-        switch (x) { // violation 'Switch can be replaced with enhanced switch.'
-            case 1: x = 1; break;
-            default: break;
+        switch (x) {
+            case 1 -> x = 1;
+            default -> {}
         }
     }
 
     void testIndentVisitorNoNewline(int x) {
         int y;
-        switch (x) { // violation 'Switch can be replaced with enhanced switch.'
-            case 1:
-                y = 1; x = 2;
-                break;
-            default:
-                y = 0;
-                break;
+        switch (x) {
+            case 1 -> {
+                y = 1; x = 2;}
+            default -> y = 0;
         }
     }
 }
