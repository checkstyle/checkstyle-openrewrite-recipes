--- src/test/resources/org/checkstyle/autofix/recipe/emptystatement/emptyforloop/InputEmptyForLoop.java
+++ src/test/resources/org/checkstyle/autofix/recipe/emptystatement/emptyforloop/OutputEmptyForLoop.java
@@ -9,12 +9,12 @@
 
 package org.checkstyle.autofix.recipe.emptystatement.emptyforloop;
 
-public class InputEmptyForLoop {
+public class OutputEmptyForLoop {
     void method() {
-        for (int i = 0; i < 10; i++); // violation 'Empty statement.'
+        for (int i = 0; i < 10; i++) {}
     }
 
     void infiniteLoop() {
-        for (;;); // violation 'Empty statement.'
+        for (;;) {}
     }
 }
